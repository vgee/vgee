# Production Environment Configuration
# This file is used when APP_ENV=prod
# IMPORTANT: Never commit real tokens to version control!

BOT_TOKEN=
DEFAULT_CHAT_ID=

# Timeout in seconds (standard for production)
DEFAULT_TIMEOUT=10

# Retry configuration
RETRY_MAX_ATTEMPTS=3
RETRY_INITIAL_DELAY=1.0
